node_modules/

# Native build outputs (Gradle/CMake) — derleme çıktısı, kaynak değil; her build'de yeniden üretilir
build/
.cxx/
.gradle/

# Ortam değişkenleri — gerçek anahtarlar; .env.example commit'lenir, .env edilmez
.env
.env.local
.env.*.local